package com.notes.hsnotes.data.db

/**
 * No converters required — every entity field uses a Room-supported primitive
 * (Long, Int, String, Boolean). File present per project layout for future expansion.
 */
class Converters
